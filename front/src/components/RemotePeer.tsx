import { Socket } from "socket.io-client";

interface IProps {
  peerId: string;
}

const RemotePeer = ({ peerId }: IProps) => {
  // Creates and handles a connection to another peer
  return <></>;
};

export default RemotePeer;
